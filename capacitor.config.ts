
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maslakedeoband.app',
  appName: 'Maslak-e-Deoband',
  webDir: 'public',

  server: {
    url: 'https://www.maslakedeoband.in',
    cleartext: false,
  },
};

export default config;


